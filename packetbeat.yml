interfaces:
  device: any

protocols:
  http:
    ports: [80, 8080, 8081, 5000, 8002]

output:

  elasticsearch:
    enabled: true
    
    host: elasticsearch
    port: 9200
    
    save_topology: true
